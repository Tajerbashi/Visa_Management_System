@page
@model SSO.Pages.Admin.UserPageModel
@{
}

<div style="height:80vh; overflow:auto;" class=" container-fluid d-flex flex-column justify-content-between">
    <div class="row shadow">
        <div class="text-center alert alert-primary border-primary fixed-top">مدیریت کاربران</div>
        <div>
            <table class="table table-hover text-center text-light">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>نام</th>
                        <th>فامیل</th>
                        <th>نام کاربری</th>
                        <th>ایمیل</th>
                        <th>رمز</th>
                        <th>تلفن</th>
                        <th>عملیات</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Users)
                    {
                        <tr>
                            <th>@item.Id</th>
                            <th>@item.Name</th>
                            <th>@item.Family</th>
                            <th>@item.UserName</th>
                            <th>@item.Email</th>
                            <th>@item.Password</th>
                            <th>@item.Phone</th>
                            <th>
                                <a class="btn btn-warning">ویرایش</a>
                                <a class="btn btn-info">نمایش</a>
                                <a class="btn btn-danger">حدف</a>
                                <a class="btn btn-primary">ورود</a>
                            </th>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <div class="row shadow text-center">
        <a asp-page="./AdminPanel" class="btn btn-dark text-light fixed-bottom p-3">مدیریت کابران</a>
    </div>
</div>